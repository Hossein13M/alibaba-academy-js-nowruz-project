//Create a function to convert color numbers from RGB into HEX code.

function ValToHex(color) {
    var hex = c.toString(16);
    return hex
}
function rgbToHex(r, g, b) {
    return("#" + ValToHex(r) + ValToHex(g) + ValToHex(b));
}
console.log("rgbToHex")